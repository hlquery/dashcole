{
  "name": "dashcole",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,dashboard,landing -c blue,green,magenta \"npm --prefix api run dev\" \"npm --prefix web run dev\" \"npm --prefix landing run dev\"",
    "build": "npm --prefix web run build && npm --prefix landing run build",
    "check": "npm --prefix api run check && npm --prefix web run build && npm --prefix landing run build",
    "start": "npm --prefix api run start"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
